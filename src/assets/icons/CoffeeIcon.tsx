import { Coffee } from "phosphor-react";
import { defaultTheme } from "../../styles/themes/default";

export const CoffeeIcon = () => (
  <Coffee weight="fill" color={defaultTheme["base-text"]} />
);
