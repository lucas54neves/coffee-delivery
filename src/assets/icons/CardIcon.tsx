import { CreditCard } from "phosphor-react";
import { defaultTheme } from "../../styles/themes/default";

export const CardIcon = () => (
  <CreditCard weight="regular" color={defaultTheme["base-text"]} />
);
