import { Package } from "phosphor-react";
import { defaultTheme } from "../../styles/themes/default";

export const PackageIcon = () => (
  <Package weight="fill" color={defaultTheme["base-text"]} />
);
