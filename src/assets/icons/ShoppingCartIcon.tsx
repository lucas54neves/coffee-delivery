import { ShoppingCart } from "phosphor-react";
import { defaultTheme } from "../../styles/themes/default";

export const ShoppingCartIcon = () => (
  <ShoppingCart weight="fill" color={defaultTheme["base-text"]} />
);
